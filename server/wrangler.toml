name = "imperium-mp"
main = "src/worker.js"
compatibility_date = "2024-09-23"

# Durable Object che gestisce una "stanza" (una partita online)
[[durable_objects.bindings]]
name = "ROOMS"
class_name = "Room"

# SQLite-backed Durable Objects: disponibili anche sul piano gratuito
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Room"]
